# Route for /break-consensus --cross (owner decision D7). Values are rows divergent-claude and
# divergent-codex of docs/contracts/model-routing.tsv; change both in the same commit.
# CLAUDE_MODEL: model of the read-only Claude worker (Workflow agent() model alias).
# CODEX_MODEL / CODEX_EFFORT: route of the Codex worker (codex exec -p toolkit-divergent).
CLAUDE_MODEL=opus
CODEX_MODEL=gpt-6-astra
CODEX_EFFORT=medium
