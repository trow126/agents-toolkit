## ワークスペース衛生

- 削除の範囲は core-contract に従う（自分が作成し、不要かつ安全に削除できると確認した一時物のみ。既存・ユーザー所有・無関係な成果物は削除しない）
- 誤ってコミットされる可能性のある一時ファイルを残さない
- 新しいファイルは既存のディレクトリパターンを確認してから配置する（テストは `tests/`, `__tests__/`, `test/`、スクリプトは `scripts/`, `tools/`, `bin/` など）
