{
  "$comment": "workspace-only READ を実現する project 設定 template(2026-07-23 H-013)。user 設定の '.' は ~/.claude に解決されるため、workspace 相対の read 境界は project 側にしか置けない(公式 sandboxing docs の recipe)。使い方: 対象プロジェクトの .claude/settings.json にこの sandbox ブロックを追加する。",
  "sandbox": {
    "enabled": true,
    "filesystem": {
      "denyRead": ["~/"],
      "allowRead": ["."]
    }
  }
}
