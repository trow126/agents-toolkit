# Codex implementer route for gh-codex-drive and gh-roadmap-drive.
# Every companion `task` launch passes these values as --model / --effort.
# Change them only through the routing promotion gate (docs/reports/owner-decisions.md);
# the companion accepts efforts none..xhigh only.
CODEX_MODEL=gpt-6-astra
CODEX_EFFORT=medium
