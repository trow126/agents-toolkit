# ワークスペースルール（Claude 固有）

<!-- 衛生管理・配置一般は ~/.agents/rules/workspace-hygiene.md、時間的認識は decision-integrity.md へ移設 -->

## ファイル配置

- Claude 固有のドキュメントは `claudedocs/` ディレクトリに配置
- 対応する Issue がクローズされたら `claudedocs/brainstorm/*.md` を削除する
